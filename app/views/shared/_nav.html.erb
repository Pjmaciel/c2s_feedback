<nav class="bg-white shadow">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center h-16">
      <%= link_to "C2S Feedback", root_path, class: "text-xl font-bold text-blue-600" %>

      <ul class="flex space-x-4">
        <% if user_signed_in? %>
          <li>
            <% if current_user.client? %>
              <%= button_to "Logout", destroy_client_session_path,
                            method: :delete,
                            data: { turbo: false },
                            class: "text-red-600 hover:text-red-800" %>
            <% else %>
              <%= button_to "Logout", destroy_employee_session_path,
                            method: :delete,
                            data: { turbo: false },
                            class: "text-red-600 hover:text-red-800" %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>