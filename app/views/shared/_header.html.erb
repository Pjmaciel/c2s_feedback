<header class="bg-white shadow">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <%= link_to 'C2S Feedback', client_dashboard_path, class: 'text-xl font-bold text-blue-600' %>
      </div>
      <div class="flex items-center">
        <div class="ml-4 flex items-center space-x-4">
          <%= button_to 'Sair', destroy_user_session_path, method: :delete, class: 'text-gray-600 hover:text-gray-900' %>
        </div>
      </div>
    </div>
  </div>
</header>